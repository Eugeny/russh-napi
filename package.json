{
  "name": "russh",
  "version": "0.0.1",
  "main": "lib/index.js",
  "license": "Apache-2.0",
  "devDependencies": {
    "@napi-rs/cli": "^2.18.3",
    "typescript": "^5.5.2",
    "rxjs": "^7"
  },
  "napi": {
    "name": "russh"
  },
  "scripts": {
    "build": "napi build --dts russh.d.ts --release && tsc",
    "build:debug": "napi build --dts russh.d.ts && tsc",
    "postinstall": "npm run build"
  },
  "peerDependencies": {
    "rxjs": "^7"
  }
}

{
  "name": "russh",
  "version": "0.0.1-alpha.4",
  "main": "lib/index.js",
  "license": "Apache-2.0",
  "dependencies": {
    "@napi-rs/cli": "^2.18.3"
  },
  "devDependencies": {
    "typescript": "^5.5.2",
    "rxjs": "^7"
  },
  "napi": {
    "name": "russh"
  },
  "scripts": {
    "build": "node build.mjs --release && tsc",
    "build:debug": "node build.mjs && tsc",
    "postinstall": "npm run build"
  },
  "peerDependencies": {
    "rxjs": "^7"
  }
}
